import React from "react";
import { AppRegistry, View, Text, StyleSheet } from "react-native";
import { styles } from "../app";

const Nav = () => ( //setup sign_out on backend
  <View style={styles.header}>
    <Text style={{color: "white"}}>SIGN OUT</Text>
  </View>
);
