defmodule DataStructures.GuessesTest do
  use ExUnit.Case, async: true
  use ExUnitProperties
  alias IslandsEngine.DataStructures.Guesses

  IO.puts "Guesses.add/3"
  property "handles hits & misses" do
    #
  end
end
