use Mix.Config

# By default, the umbrella and its children will require this file,
# ensuring they all use the same configuration.
import_config "../apps/*/config/config.exs"

# While one could configure all applications here, we prefer to delegate.
#
# Define config HERE to override imported.
